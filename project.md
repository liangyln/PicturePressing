#图片压缩软件 ##功能
###1.用户界面分三部分 ####第一部分是路径的输入，包括 1.要处理的图像所在的文件夹路径，即输入路径；2.压缩后图片的储存路径，即输出路径。3.要有一个“包含所有子文件夹”的勾选项供用户选择。4.要有一个覆盖原文件的勾选项。 ####第二部分是输出文件的命名规则，这里要有前缀、后缀以供用户选择输入，还要有保持原来名字的选项。 ####第三部分是选择压缩后文件的储存类型以及可供用户选择的压缩比例。
###2.我希望用 node.js 做用户界面，用 python 写代码
###3.如果用户勾选了“包含所有子文件夹”，那么软件需对输入路径的所有子文件夹中的图片都要进行压缩。 ##更新 1：
###1.路径的输入中，除了用文件树的形式选择路径外，也要允许用户直接粘贴路径
###2.在处理的过程中，进度条并没有随着任务的进行而变化，只是最后完成的时候突然从 0 变成 100%，这里需要升级。 ##更新 2：
###1.压缩比和压缩效果主要取决于编码器（MozJPEG、WebP、AVIF、pngquant）和参数策略，我希望在第三部分增加编码器以及相应输出文件格式的选择。2.需要在第三部分增加一个预览的功能，当用户选择了编码器、压缩质量和尺寸比例等参数后，点击预览，能弹出一个窗口对比原始图片和压缩后的图片 
###预览功能失败。我输入选择照片后，反馈文件不存在，但这个很明显是出错了。我需要你做以下修改：点击预览后，第1步，检测用户是否输入了要处理的图像所在的文件夹路径（即输入路径），以及检测用户是否选择了编码器、压缩质量和尺寸比例等参数，如果没有，请弹出窗口，告知用户需选择后才能预览，如果已经输入或勾选上述参数，进入第2步
自动读取用户的输入路径，然后读取“输入路径”中的第一张照片；第3步，弹出一个窗口，这个窗口左边是原始图片，右边是压缩后的图片。
###软件有几个问题需要升级：1.gui中按钮、文件栏等的字体是默认是白色，但实在看不清，需要全部改成黑色。2.gui中的按钮样式不太好，按钮背景颜色和文字搭配比较奇怪。3.目录树选择到文件夹后，要点击“选择当前文件夹：***”后才能点击“确定”按钮，这一点不符合一般的使用习惯，应该是用户选到哪一级目录，就可以直接点击“确定”。4.预览的功能存在bug，选择了“pngquant — 输出 .png（无损压缩）”后再点击预览，将会出现死机的问题。输出为png格式时，软件可以默认一个无损压缩的建议参数，但也要允许用户调整压缩质量。
###更新后出现bug，点击预览后，一直停留在“正在生成预览”和“加载中”
###出现bug：1.点击“pngquant — 输出 .png（无损压缩）”后图片不仅没有压缩，反而还增大了。2.点击“AVIF — 输出 .avif（体积最小）”后，预览窗口报错“failed to encode image".3.预览生成图片的速度比之前慢，需要看看是什么问题，能否改进。
###预览功能上需要更新：预览时应该允许放大图片，以便更好地对比压缩前后的照片。用户点击图片时，图片能像windows中的照片应用那样弹出，随后鼠标中键可以放大、缩小，图片边缘有左、右的箭头供用户点击查看下一张或上一张图片，用户也能用键盘的左、右方向键切换上下图片。在切换图片时，保持同样的大小比例（即如果用户局部放大到150%，切换的时候图片位置不变，放大的比例不变）
###更新：1.弹出的预览窗口应在最上层，而不是在软件主体下层。2.主程序的gui中，各部分的空隙减少，保证不需要下滑就能显示所有内容。3.选择路径时，点击浏览后弹出的目录树中，字体颜色看不清，需要全部改为自适应黑/白
###需要有以下的更新：1.主程序的gui应保留可滚动的选项，gui中的文字、按钮应该自适应大小，以便适配用户对gui的放大和缩小。2."开始压缩"的图标太小，不好点击，需要调整。
###更新：预览的窗口中：1.缩放的中心应该是鼠标所在的位置，而非图片的绝对中心。2.允许用户用鼠标左键框选放大，框选时鼠标变为加号；允许用户用鼠标右键缩小，中键平移（pan）

